---
- name: play book to add alist of users
  hosts: webservers
  vars_files:
    - vars/vars.yml
  tasks:
   - name: addu sers
     ansible.builtin.user:
      name: "{{item.name}}"
      uid: "{{item.id}}"
     loop: "{{user_list}}"